<Window x:Class="Eugene.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:Eugene.Desktop"
        xmlns:commands="clr-namespace:Eugene.Desktop.Commands"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="Exit" Command="{Binding ExitApplicationCommand}" />
            </MenuItem>
            <MenuItem Header="Dataset">
                <MenuItem Header="Load" Command="{Binding LoadDatasetCommand}" />
                <MenuItem Header="Save" Command="{Binding SaveDatasetCommand}" />
                <MenuItem Header="Import" Command="{Binding ImportDatasetCommand}" />
                <MenuItem Header="Export" Command="{Binding ExportDatasetCommand}" />
                <Separator />
                <MenuItem Header="Generate Random" Command="{Binding GenerateDatasetCommand}" />
            </MenuItem>
            <MenuItem Header="Optimization">
                <MenuItem Header="Start" Command="{Binding StartOptimizationCommand}" />
            </MenuItem>
            <MenuItem Header="Result">
                <MenuItem Header="Save" Command="{Binding SaveResultCommand}" />
                <MenuItem Header="Export" Command="{Binding ExportResultCommand}" />
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="3" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical">
                <GroupBox Header="Blockers" MinHeight="200">
                    <xctk:CheckListBox ItemsSource="{Binding Dataset.Blockers}" DisplayMemberPath="Name" SelectedItemsOverride="{Binding SelectedBlockers}" />
                </GroupBox>
                <GroupBox Header="Resolution" MinHeight="200">
                    <xctk:PropertyGrid SelectedObject="{Binding Result}" />
                </GroupBox>
            </StackPanel>

            <GridSplitter Grid.Column="1" ResizeDirection="Columns" HorizontalAlignment="Stretch" />

            <DataGrid Grid.Column="2" ItemsSource="{Binding DatasetAsTable.DefaultView}"
                      AutoGenerateColumns="True"
                      CanUserSortColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      />

        </Grid>

        <StatusBar Grid.Row="2">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem>
                <TextBlock Name="lblStatusDataset" />
            </StatusBarItem>
            <Separator Grid.Column="1" />
            <StatusBarItem Grid.Column="2">
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
            <Separator Grid.Column="3" />
            <StatusBarItem Grid.Column="4">
                <ProgressBar Minimum="0" Maximum="100" IsIndeterminate="{Binding IsOptimizing}" Width="90" Height="16" />
            </StatusBarItem>
        </StatusBar>
        
    </Grid>
</Window>
